# data & artifacts
# data/  -> private HF dataset (images; not in git). results/ -> git `results` branch.
# manifests/ ARE committed (small CSVs; define the splits for reproducibility).
data/
results/
results-download/
*.pt
*.pth
*.ckpt
wandb/

# python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# secrets — never commit tokens
.env
.env.*
# ...but DO commit the template:
!.env.example
*token*
*secret*
results-download/

# tool / session state
.aris/
.claude/

# os
.DS_Store
